#pragma once

// Skeleton hunt (2026-09-11). '\' captures the character objects the camera
// controllers follow plus every pointed-to block that looks like a joint
// matrix array, to re5vr_skel.bin. See skeleton_probe.cpp.
void SkeletonProbe_OnEndScene();
